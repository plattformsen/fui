/**
 * Shared constants and other utilities for the Jameson's
 * Fresh UI project.
 *
 * Currently, only {@link DEFAULT_NAME_EXAMPLE} is defined.
 *
 * ```tsx
 * const name = undefined;
 * return (
 *  <p>Hello, {name ?? DEFAULT_NAME_EXAMPLE}!</p>
 * )
 * ```
 *
 * @module
 */

/**
 * The default name example used in the `@fui/hello`
 * component.
 */
export const DEFAULT_NAME_EXAMPLE = "World";
